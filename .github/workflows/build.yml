name: Build Executables

on:
  push:
    tags:
      - 'v*'  # Triggers on version tags like v1.0.0, v1.2.3, etc.

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Build Windows executable
        run: python build_exe.py
      
      - name: Upload Windows artifact
        uses: actions/upload-artifact@v3
        with:
          name: ImageUploader-Windows
          path: dist/ImageUploader.exe

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Build macOS executable
        run: python build_exe.py
      
      - name: Create DMG (optional)
        run: |
          mkdir -p dist/dmg
          cp -r dist/ImageUploader.app dist/dmg/
          hdiutil create -volname "ImageUploader" -srcfolder dist/dmg -ov -format UDZO dist/ImageUploader.dmg
      
      - name: Upload macOS artifact
        uses: actions/upload-artifact@v3
        with:
          name: ImageUploader-macOS
          path: |
            dist/ImageUploader.app
            dist/ImageUploader.dmg

  create-release:
    needs: [build-windows, build-macos]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Download Windows artifact
        uses: actions/download-artifact@v3
        with:
          name: ImageUploader-Windows
          path: artifacts/windows
      
      - name: Download macOS artifact
        uses: actions/download-artifact@v3
        with:
          name: ImageUploader-macOS
          path: artifacts/macos
      
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/windows/ImageUploader.exe
            artifacts/macos/ImageUploader.dmg
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}